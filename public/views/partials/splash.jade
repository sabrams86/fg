#splash-page
  header.splash-header
    .container
      h2.title.left
        | FriendlyGear
        small  a place for friends with gear {{$root.userLoggedIn}}
      user-auth-directive.splash-user-menu
  main.splash-main
    .splash.container
      br
      .splash-form.ui.three.column.grid
        .row
          .column
            label(for='when') When do you want to go?
          .column
            label(for='what') What do you want to do?
          .column
            label(for='where') Where do you want to do it?
        .row
          .column
            .ui.two.column.grid
              .column
                input#startDate(type='date', ng-model='startDate' name='mainSearch[startDate]')
              .column
                input#endDate(type='date', ng-model='endDate' name='mainSearch[endDate]')
          .column
            searchable-category-dropdown(ng-model='foo')
          .column
            input(type='text', ng-model='location' name='mainSearch[location]')
        .row
          .column
          .column
            button.row.btn(type="button" ng-click="search()") Search
  footer#splash-footer
    a(href='#') How it works 
    |  | 
    a(href='#' ng-click="signUp()") Sign up
    |  | 
    a(href='#') Log in
    |  | 
    a(href='/home') Browse all gear
